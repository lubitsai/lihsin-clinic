# ===== 立欣診所網站 _headers（Netlify 安全標頭設定）=====
# 套用到全站所有頁面，提升安全性與專業度

/*
  # 防止網站被嵌入他人 iframe（避免點擊劫持 clickjacking）
  X-Frame-Options: SAMEORIGIN
  # 防止瀏覽器誤判檔案類型（MIME 混淆攻擊）
  X-Content-Type-Options: nosniff
  # 控制 referrer 資訊外洩程度（保護使用者隱私）
  Referrer-Policy: strict-origin-when-cross-origin
  # 強制瀏覽器後續一律走 HTTPS（含子網域），效期一年
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  # 限制可使用的瀏覽器功能（診所網站不需要這些感測器/裝置權限）
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=()

# ── 靜態資源快取（圖片、PDF）：加速回訪載入 ──
/images/*
  Cache-Control: public, max-age=2592000

/docs/*
  Cache-Control: public, max-age=2592000

# ── IndexNow 金鑰檔：確保純文字、可被搜尋引擎讀取 ──
/631da727697dc88a64a8f0b76042c891.txt
  Content-Type: text/plain; charset=utf-8

# 註：Content-Security-Policy(CSP) 尚未上線。外部相依現況（2026-07-10）：
# CDN Tailwind 已於 2026-06-20 遷移為自載 /tailwind.css、cdnjs(Font Awesome)
# 已於 2026-07-04 移除；現存外部來源為 Google Fonts、Google Analytics(gtag)、
# Chatbase widget。CSP 草案（_headers_proposed.txt，院長留存；internal/00 §6
# 待辦）需先過 Netlify Deploy Preview 驗證後才可 merge，勿直接上線。
