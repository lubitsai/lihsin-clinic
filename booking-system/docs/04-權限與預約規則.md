# 04｜權限矩陣與預約規則定義

## 一、角色權限矩陣

| 功能 | 一般民眾 | 櫃檯人員 STAFF | 系統管理員 ADMIN | 醫師唯讀 DOCTOR_READONLY |
|---|---|---|---|---|
| 線上預約／查詢／取消自己的預約 | ✅ | — | — | — |
| 查看每日預約名單 | — | ✅ | ✅ | ✅（僅自己） |
| 搜尋病人與預約 | — | ✅ | ✅ | — |
| 代約／修改／取消／改期 | — | ✅ | ✅ | — |
| 報到／完成／未到標記 | — | ✅ | ✅ | — |
| 覆寫同日／同時筆數限制（需理由） | — | ✅ | ✅ | — |
| 替受限病人代約（需理由） | — | ✅ | ✅ | — |
| 排班、休診、特殊營業時間 | — | ✅ | ✅ | — |
| 查看未到次數與限制狀態 | — | ✅ | ✅ | — |
| 黑名單解除／重設未到次數 | — | — | ✅ | — |
| 員工帳號與權限管理 | — | — | ✅ | — |
| 預約規則（系統設定） | — | — | ✅ | — |
| 病歷合併 | — | — | ✅ | — |
| 完整稽核紀錄 | — | — | ✅ | — |
| LINE／通知設定 | — | — | ✅ | — |
| 查看完整（未遮罩）證件號 | — | 額外權限 `pii:full` | ✅ | — |

- 權限以 `staff_roles.permissions`（字串陣列）儲存，後端每個 action 檢查；櫃檯人員無法建立/修改/停用任何帳號（含自己），不可觸碰 ADMIN 帳號。
- 管理員登入強制 TOTP 兩步驟驗證；所有員工閒置 30 分鐘自動登出（可設定）。

## 二、預約狀態機

```
PENDING(待確認) → CONFIRMED(已確認) → CHECKED_IN(已報到) → COMPLETED(已完成)
      │                │                    │
      ├→ CANCELLED_BY_PATIENT(病人取消)      │
      ├→ CANCELLED_BY_CLINIC(診所取消)       │
      ├→ RESCHEDULED(已改期→產生新預約)       │
      └→ NO_SHOW(未到)  ←──────────────────┘（過號未報到由櫃檯標記）
```

- **有效預約**（占名額、計入同日與同時筆數限制）：`PENDING`、`CONFIRMED`、`CHECKED_IN`。
- **占用歷史名額**（不可再被預約走）：上列加 `COMPLETED`、`NO_SHOW`。
- 取消／已改期立即釋放名額。
- 所有狀態變更寫入 `appointment_status_history` 與 `audit_logs`。

## 三、預約規則（後端一律重新檢查）

| 規則 | 預設值 | 設定鍵（system_settings） |
|---|---|---|
| 滾動開放天數 | 14 天 | `booking.open_days` |
| 每日開放時間 | 00:00（最新一天於此時間才開放） | `booking.open_time` |
| 預約截止 | 時段開始前 240 分鐘（4 小時，同官網公告） | `booking.same_day_cutoff_minutes` |
| 同日上限 | 1 筆有效預約／日曆日 | `booking.daily_limit`（固定 1，保留鍵） |
| 同時未完成預約上限 | **每個預約帳號** 2 筆（當日的預約不計入） | `booking.active_max` |
| 同行家人上限 | 3 位（家庭代表預約，共用 1 個名額） | `booking.max_companions` |
| 未到限制門檻 | 累計達 3 次即自動暫停（同官網公告） | `booking.no_show_threshold` |
| 暫停天數 | 90 天，期滿自動恢復並將未到次數歸零（0＝需人工解除） | `booking.no_show_suspension_days` |
| 取消/改期截止 | **該診次開診前**（開診時間＝當日班表該診別最早的時間） | ——（由班表推算） |
| ↳ 判斷單位 | **一律以診次為單位**，不另看個別時段的時間（院長 2026-08-06 確認） | ——（由班表推算） |
| ↳ 查無診次時的備援 | 看診前 120 分鐘（該診別當日整個不在班表上時才會用到） | `booking.cancel_cutoff_minutes` |
| 開診後封鎖 | 該診次開診後不再受理該診次的預約（櫃檯代約不受限） | ——（由班表推算） |
| 報到保留 | 時段開始後 10 分鐘；逾時由櫃檯總覽標記，**系統不自動取消** | `booking.checkin_grace_minutes` |
| 每 30 分鐘名額 | 1 名/醫師（時段可手動加開） | `booking.default_slot_capacity` |
| 當日提醒開關 | **關**（院長 2026-08-05：提醒只發一次，報到規則併入前一日那則） | `notify.same_day_reminder` |
| 通知訊息長度 | **所有簡訊壓在單則 70 字內**（不含預約編號／醫師／門診別）；LINE 版保留完整 | —— |
| 預約編號 | 不寫進任何民眾通知（院長 2026-08-05）。仍會在預約完成頁與「我的預約」顯示，供家長來電時報號；櫃檯搜尋框也吃這個號碼 | —— |
| 前日提醒時間 | **20:00** | `notify.day_before_time` |
| 當日提醒時間 | 08:00（目前未使用，開啟當日提醒才生效） | `notify.same_day_time` |
| 閒置登出 | 30 分鐘 | `security.staff_idle_minutes` |

### 時段格點：一律整點或半點

本院時段固定落在整點與半點，每 30 分鐘一組名額。時段清單是從班表的 `startTime`
起每 30 分鐘推算，因此班表若被填成 08:15，整天就會變成 08:15／08:45／09:15…，
與公告的「每 30 分鐘為一個時段」不符；區間結束時間非格點時還會產生不足 30 分鐘的畸零時段。

平日三診次實際產生的時段（院長 2026-08-05 確認）：

| 診次 | 看診區間 | 可預約時段 |
|---|---|---|
| 早診 | 08:00–12:00 | 08:00、08:30、09:00、09:30、10:00、10:30、11:00、11:30（8 個）|
| 午診 | 14:30–18:00 | 14:30、15:00、15:30、16:00、16:30、17:00、17:30（7 個）|
| 晚診 | 18:30–21:30 | 18:30、19:00、19:30、20:00、20:30、21:00（6 個）|

區間**含頭不含尾**：21:30 是收診時刻，最後一個時段是 21:00（看到 21:30）。
週六、週日依官網 SCHEDULE 較短（六 08:00–11:30／14:30–18:00、無夜診；
日 08:00–11:30／18:30–21:00、無午診），時段數隨之減少，格點規則相同。

規則在**寫入排班與名額**這一層強制（`slotTimeSchema`），涵蓋四個入口：
週班表、日期例外（特殊時間／加診／封鎖時段）、手動加開名額、門診類型可預約時間窗。
非格點時間會直接被拒絕並提示「時間需為整點或半點」。
預約端不需另外檢查——時段本來就只能從班表產生的清單中挑。

### 診次（session）如何判定

官網公告有兩條規則以「診次」為單位——開診後不再受理該診次的預約、開診前都可自行取消改期。
系統**不另建診次資料表**：當日班表中該診別最早的 `startTime` 就是開診時間
（`getSessionStartTimes`／`sessionStartsOf`），因此停診、代診、特殊時間等日期例外會自動反映，
臨時改開診時間不必再改一次設定。查不到診次時（櫃檯在班表外手動加開的時段）退回分鐘數備援。

### 現行門診時間表（與官網 index.html 門診時間表一致）

| 診次 | 週一 | 週二 | 週三 | 週四 | 週五 | 週六 | 週日 |
|---|---|---|---|---|---|---|---|
| 早診 | 蔡 | 蔡 | 蔡 | **李** | 蔡 | 蔡<br>–11:30 | **蔡＋李**<br>–11:30 |
| 午診 | 蔡 | 蔡 | 蔡 | 蔡 | **李** | **李** | 休診 |
| 晚診 | **蔡＋李** | 蔡 | **李** | 蔡 | 蔡 | 休診 | 蔡<br>–21:00 |

時間：早診 08:00–12:00、午診 14:30–18:00、晚診 18:30–21:30
（週六早診至 11:30、週日早診至 11:30、週日晚診至 21:00）。

**雙診只有兩處**：週一晚診、週日早診。這兩個診次每 30 分鐘有 2 個名額（兩位醫師各 1），
其餘診次皆為單診、每 30 分鐘 1 名。

> **官網為唯一事實來源（2026-08-05 起）**：班表不再手動維護兩份。
> `internal/tools/sync_schedule.py` 解析官網 `index.html` 的門診時間表，產出
> `prisma/schedule.json`；seed 與 `scripts/sync-schedule.ts` 都讀這一份。
> 官網改了而沒同步，`sync_schedule.py --check` 會擋下（已寫入 `clinic-schedule` skill 的交付尾段）。
>
> 同步涵蓋週班表與單日特例（官網 `EXCEPTIONS`）：整日休診、某診次停診、當日特殊時間都會自動建立。
> **同步不到的是「某醫師代診」**——官網只有時間、沒有醫師，代診請在後台「排班管理」另行建立。
> 櫃檯在後台輸入的例外不會被同步覆蓋（只清 `createdBy = website-sync` 的那些）。
>
> **既有預約會失去時段時，同步整批不寫入**（院長 2026-08-06 裁示 (a)，比照單日例外）：
> 命令列印出受影響名單並以 exit code 2 結束；櫃檯逐筆改期後再跑一次，或
> `--cancel-affected` 一併取消並通知。後台直接改／刪週班表也走同一套（畫面列名單）。
> 細節見 `09` D12。

### 預約成立方式

四種門診一律**送出即成立**（狀態 `CONFIRMED`），不需櫃檯審核——院長 2026-08-05 裁示。
`requiresReview` 欄位保留，後台可逐科改回「需櫃檯確認」（改回後該科新單為 `PENDING`，
前台會顯示「此門診送出後需櫃檯確認」）。

### 各門診可預約的醫師（院長 2026-08-05 指定）

| 門診 | 可預約醫師 |
|---|---|
| 一般門診 | 蔡醫師、李醫師 |
| 兒童發展篩檢 | 蔡醫師、李醫師 |
| 減重特別門診 | 蔡醫師 |
| 過敏特別門診 | 蔡醫師 |

存於 `clinic_type_doctors`，後台「系統設定 → 門診類型 → 可接受預約的醫師」可調整。
班表（醫師何時看診）與此無關，兩位醫師仍各自排班。
該門診只有一位醫師時，前台不顯示「不限醫師」——兩個選項結果相同。

**開放時段**：一般門診、減重、過敏三科不另設星期／診別限制
（`allowedWeekdays`／`allowedSessions` 留空），可預約時段直接跟著該門診醫師的班表走——
減重與過敏因此等同「蔡醫師有開診就開放」。兒童發展篩檢另以時間窗限定（見下）。

兩條規則交叉後的實際結果（值得注意，避免誤以為是漏排）：

- **減重、過敏在蔡醫師沒有診的時段就沒有名額**：週三晚診、週四早診、週五午診、週六午診皆無。
- **兒童發展篩檢在週四早上與週五午診會是李醫師施測**（該診次只有李醫師），
  兩位醫師都在篩檢的醫師名單內，故仍可預約。
- 週六、週日無篩檢時間窗，故不開放。

### 放假日（國定假日與例假日）

依行政院人事行政總處「政府行政機關辦公日曆表」，收錄表上**所有放假日**
（院長 2026-08-05 指示：放假日一律不做兒童發展篩檢）。
診所放假日**多半照常看診**，只有標記「國定假日停開」的門診當天不開放線上預約——
目前只有兒童發展篩檢（官網公告：「國定假日不施測」）。
門診本身若也有異動，另走官網單日公告（見上）。

- 資料表 `public_holidays`。具名假日以官方名稱記錄，一般例假日記為「例假日」。
- **115（2026）年已內建**：`prisma/holidays/2026.csv` 共 120 天（具名 21、例假日 99），
  與人事行政總處公布的「115 年總放假 120 天」相符。
- **116（2027）年已內建**（院長 2026-08-07 提供日曆表圖檔）：`prisma/holidays/2027.csv`
  共 **121 天**＝例假日（六、日）104 天 ＋ 平日假 17 天，全年無補班日。
  平日假 17 天逐項：開國紀念日 1/1；春節 2/4–2/10（含除夕 2/5 與 2/9、2/10 補假）；
  和平紀念日補假 3/1；清明節 4/5、兒童節補假 4/6；勞動節補假 4/30；端午節 6/9；
  中秋節 9/15、教師節 9/28；國慶日補假 10/11、臺灣光復暨金門古寧頭大捷紀念日 10/25；
  行憲紀念日補假 12/24；開國紀念日（117 年）補假 12/31。
- 兩份都由 seed 自動匯入，新部署即生效。
- 次年度（117／2028）匯入：`npx tsx scripts/import-holidays.ts <官方 CSV> --source dgpa-117`
  （官方欄位 `西元日期,星期,是否放假,備註`，補行上班的週六為 0 會正確排除；
  亦接受 `YYYY-MM-DD,名稱` 的簡易格式）。官方每年約 6 月公布次年日曆表。
  也可比照 `2027.csv` 直接建檔放進 `prisma/holidays/`，seed 會一併匯入。
- 後台「系統設定 → 國定假日行事曆」顯示已匯入範圍；**涵蓋範圍短於開放預約範圍時會出現警告**。
- 週六、週日本來就沒有篩檢時間窗，收錄例假日不改變現況；意義在於規則寫成「放假日就不施測」，
  日後若調整時間窗也不會漏掉。
- **未匯入或超出涵蓋範圍的日子一律當平日處理**（篩檢照常開放）。
  這是刻意的：缺資料就把整段時間關掉會讓篩檢完全約不到，寧可讓櫃檯看到警告後補匯入。
- 櫃檯代約不受此限——現場仍可安排。

### 門診類型可預約時間窗

`clinic_type_windows`（星期 × 時間區間）用來表達逐日不同的開放時間，
例如公告的兒童發展篩檢：週二至週五上午 09:00–11:30、下午 14:30–16:00；週一僅下午 14:30–16:00。
區間**含頭不含尾**（09:00–11:30 可約到 11:00 的時段）。
**設了時間窗就只看時間窗**，`allowedWeekdays`／`allowedSessions` 不再套用——
兩套條件相乘容易得出沒人預期的結果。

### 名額與「不限醫師」分配

1. 時段一律綁定醫師；雙診＝同一時間兩位醫師各有自己的時段，各 1 名。
2. 不得因雙診將兩名病人放入同一醫師同一時段。
2b. **名額跨門診類型共用**：一位醫師同一個 30 分鐘只會有一位病人，不分門診類型。
   例：週二午診 14:30 已有人掛過敏門診，該時段的一般門診／減重／發展篩檢對**這位醫師**即無名額
   （畫面顯示為已額滿、不可點選；同時段的另一位醫師不受影響）。
   剩餘名額是以（醫師, 日期, 時段）統計、不帶門診類型條件，資料庫的 partial unique index 為第二道防線。
3. 「不限醫師」：列出該時間有剩餘名額的醫師 → 選**當日已預約數較少**者；平手時取顯示順序在前者。
4. 後台可封鎖單一時段（`is_blocked`）或手動加開名額（記錄操作者、時間、原因）。

### 額度歸屬與家庭代表預約（院長 2026-08-01 裁示）

- 額度以「**預約帳號**」為單位，與官網公告一致：LINE 登入＝該 LINE 帳號、手機驗證＝該手機號碼。
- 櫃檯代約沒有帳號（`accountKey` 為 null），不占任何帳號額度、也不受此限。
- 超額時**擋下並說明**，不自動取消民眾既有的預約。
- **家庭代表預約**：同診次多位家人可由 1 位代表預約，共用 1 個時段名額、只計 1 筆帳號額度；
  同行者仍逐一檢查同日唯一與受限名單。兒童發展篩檢（每時段只 1 位施測）不適用。

### 病人識別

- 預約限制以「實際看診病人」為單位：`(證件類型, 證件號雜湊)` 唯一識別，生日交叉驗證。
- 同一 LINE 帳號可替多位家庭成員預約，各成員限制各自計算。
- 台灣身分證驗證檢查碼；居留證（新式統一證號）驗證檢查碼；護照僅格式檢查。

### 訊息文案（可見文字，上線前需院長核可）

- 同日重複：「您當天已有預約，無法重複預約其他門診或時段。如需更改，請先取消原預約或聯絡立欣診所。」
- 同時筆數超限：「為維護預約公平性，同時最多只能保留 2 筆尚未完成的預約（當日的預約不計入）。請先取消或完成既有預約後再約，如有特殊需求請致電立欣診所。」
- 受限名單：「目前無法使用線上預約，請致電立欣診所，由櫃檯人員協助。」（前台不出現「黑名單」字樣）
- 身分探測防護:查詢失敗一律回覆「查無符合的預約資料，請確認輸入內容」，不透露特定證件號是否存在。
