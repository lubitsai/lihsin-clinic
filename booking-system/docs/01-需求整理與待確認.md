# 01｜需求整理與待診所確認事項

> 立欣診所自有線上預約系統。本文件整理需求書全文的落地方式，以及**仍需院長／診所確認的設定**。
> 系統原則：所有程式碼、資料庫、第三方帳號（LINE、主機）由立欣診所自行持有；不依賴 BookNow 或任何預約 SaaS。

## 一、範圍總覽

| 模組 | 內容 | 狀態 |
|---|---|---|
| 前台 | 七步驟預約流程、查詢/取消/改期、LINE 登入（唯一身分）、規則與個資告知 | ✅ 已實作 |
| 後台 | 今日預約總覽（雙醫師欄）、搜尋、代約/改期/報到/未到、排班與例外、病人與黑名單、員工帳號、系統設定、稽核紀錄 | ✅ 已實作 |
| 預約引擎 | 交易 + 資料庫唯一性約束；名額、同日、同時 2 筆未完成預約、黑名單、14 天滾動開放 | ✅ 已實作＋自動化測試 |
| LINE | LINE Login OAuth（線上預約與查詢的唯一入口）、Messaging API 推播（成立/異動/取消/提醒） | ✅ 已實作（需填正式金鑰） |
| 安全 | 密碼 bcrypt、管理員 TOTP 2FA、身分證 AES-256-GCM 加密＋HMAC 雜湊索引、遮罩、rate limit、稽核 | ✅ 已實作 |
| 部署 | Docker Compose（app + PostgreSQL）、備份/還原腳本與說明 | ✅ 已提供 |

## 二、關鍵規則落地方式（摘要，詳見 04）

- **名額**：每醫師每 30 分鐘 1 名（可由後台加開）；雙診＝兩醫師各自 1 名，時段綁定醫師；「不限醫師」自動分配並平衡兩位醫師人數。
- **同日唯一**：同一病人（以證件號＋生日識別，非 LINE 帳號）同一日曆日僅 1 筆有效預約（待確認＋已確認＋已報到）；資料庫 partial unique index 強制。
- **同時 2 筆（不含當日）**：同一病人同時最多保留 2 筆尚未完成的預約（待確認／已確認／已報到），**當日的預約不計入**；跨醫師、跨門診合併計算，上限可於後台設定（`booking.active_max`）。與官網公告（BookNow 現行規則）一致，2026-07-31 由院長裁示統一。
- **14 天滾動開放**：預設每日台灣時間 00:00 開放第 14 天；天數可設定。
- **未到黑名單**：未到累計超過 3 次（第 4 次）自動限制前台預約；門檻可設定；後台可解除／暫時解除／重設，需輸入原因並留稽核。
- **超賣防護**：預約建立/改期/取消皆為單一交易；時段列 `SELECT … FOR UPDATE` 序列化 + partial unique index 雙保險。

## 三、⚠️ 仍需診所（院長）確認的設定

上線前請逐項確認，均可於後台「系統設定」直接調整，程式碼不需改動：

1. ~~**同日/當天可否預約**~~ **已依官網公告結案**：開放當日預約，截止為時段開始前 4 小時
   （`allow_same_day=true`、`same_day_cutoff_minutes=240`），另加「該診次開診後不再受理」。
2. ~~**取消/改期截止時間**~~ **已依官網公告結案（2026-08-04）**：改以「該診次開診前」為準；
   `cancel_cutoff_minutes` 降為備援（僅班表外手動加開的時段會用到）。
3. ~~**每 30 分鐘名額**~~ **已裁示（2026-08-05）**：維持 1 名／醫師。
4. ~~**7 天上限**~~：**已結案（2026-07-31）**——院長裁示改為「同時 2 筆未完成預約、當日不計入」，與官網公告一致。
   ✅ **官網公告其餘條文（原 10 項差異）已於 2026-08-01～08-05 全數處理**，逐條見 `09-官網公告與系統規則對照.md`。
5. ~~**未到門檻**~~ **已依官網公告結案**：累計「達」3 次即暫停，暫停 90 天後自動恢復並歸零。
6. ~~**待確認 vs 直接確認**~~ **已裁示（2026-08-05）**：四種門診一律**送出即成立**（已確認），
   特別門診不再需要櫃檯審核。（`requiresReview` 全為 false；後台仍可逐科改回。）
7. **特別門診開放時段**：
   - ✅ **醫師已定案（院長 2026-08-05）**：一般門診＝蔡醫師、李醫師；兒童發展篩檢＝蔡醫師、李醫師；
     減重特別門診＝蔡醫師；過敏特別門診＝蔡醫師。已寫進 seed，後台「系統設定 → 門診類型」可改。
   - ✅ **兒童發展篩檢時段已定案**：週一至週五（國定假日除外）上午 09:00–11:30、下午 14:30–16:00，
     週一上午不施測。已以 `clinic_type_windows` 實作。
   - ✅ **減重、過敏開放時段已定案（院長 2026-08-05）**：蔡醫師有開診的時段皆開放，
     不另設星期／診別限制（`allowedWeekdays`／`allowedSessions` 留空），可預約時段直接跟著蔡醫師的班表走。
     （原設為「需櫃檯確認」，院長同日另裁示四科一律送出即成立，見第 6 項。）
   > **本項全部結案。**
   > **「國定假日不施測」已自動化並內建（2026-08-05）**：依院長提供的辦公日曆表建檔
   > （`prisma/holidays/2026.csv` 120 天、`prisma/holidays/2027.csv` 121 天），seed 自動匯入；
   > 放假日當天篩檢不開放、其餘門診照常。
   > **補假日同樣停開（院長 2026-08-16 確認）**：判斷看日期有沒有在放假日表內、不看名稱，
   > 節日本身與其補假一視同仁（2026 年 5 天、2027 年 8 天，皆落在週一至週五）；
   > 補行上班的週六屬上班日，照常施測。
   > **116（2027）年已於 2026-08-07 補建，涵蓋到 2027-12-31**；
   > 下一次要處理是 117（2028）年，官方約 2027 年 6 月公布。
   > **班表已依院長 2026-08-05 提供的現行門診時間表寫入 seed**（與官網那張表逐格一致）：
   > 多數診次單診，雙診只有週一晚診與週日早診。詳見 `04-權限與預約規則.md`。
8. ~~**醫師唯讀帳號**~~ **已裁示啟用（2026-08-05）**：seed 建立 `dr-tsai`／`dr-lee` 兩組唯讀帳號
   （只能看自己的預約，不能代約／改期／取消）。預設密碼 `lihsin-doctor-2026`，
   可用 `SEED_DOCTOR_PASSWORD` 覆寫；**上線後請立即改密碼**。
9. ~~**簡訊供應商**~~ **已裁示取消（2026-08-13）**：為節省簡訊費用並配合診所現行運作，
   取消簡訊通知與簡訊系統商合作，**所有通知改走 LINE**，前台身分驗證也改為 LINE 登入。
   影響：沒有使用 LINE 的家長無法線上預約，改走電話或現場掛號（由櫃檯以後台代約）。
   **2026-08-15 補充裁示**：既有病歷的 LINE 綁定改為**櫃檯核對後才能建立**
   （民眾自助綁定已移除），避免他人冒用證件綁走病歷身分。櫃檯操作見 `07` §六之二。
10. **LINE 官方帳號**：需以診所帳號建立 LINE Login channel 與 Messaging API channel（設定步驟見 `05-LINE串接設定.md`）。
    **簡訊取消後這項變成上線的必要條件**——沒有它，家長既無法線上預約，也收不到任何通知。
11. ~~**看診提醒**~~ **已裁示（2026-08-05）**：只發一次，於**前一日 20:00**；當日提醒關閉。
    （`day_before_time=20:00`、`same_day_reminder=false`；crontab 只需一行 `0 20 * * *`。）
12. ~~**個資保存期限**~~ **已裁示（2026-08-05）**：維持 3 年（`privacy.retention_years=3`）。
13. **前台文案**：所有可見文字（首頁提醒、規則頁、錯誤訊息、通知範本）依內部合規制度，正式上線前需院長逐字核可。**逐條核對表已備妥：`08-前台文案核對表.md`**（含合規自查結果，院長只需標出要修改的項目編號）。
14. ~~**網域**~~ **已釐清（2026-08-05）**：`booking.lhpedclinic.com.tw` 是既有網域的子網域，
    **不需另外購買**；只要在管理 `lhpedclinic.com.tw` DNS 之處新增一筆 A 紀錄指向主機即可，
    HTTPS 憑證由 Caddy 自動申請（Let's Encrypt，免費）。⏳ 待主機備妥後建立該筆 DNS 紀錄。
15. ~~**主機**~~ **已決定（院長 2026-08-06 改定）**：**Google Cloud Compute Engine 虛擬機**，
    `e2-medium`（2 vCPU / 4GB）、Ubuntu 24.04 LTS、50GB `pd-balanced`、
    **`asia-east1`（彰化）機房——資料留在台灣**。
    建立與強化步驟見 `06-部署與備份.md` §2-0。
    個資告知**維持現行文字**（院長 2026-08-05 裁示；主機仍為診所自行持有與管理，
    見 `08` §四 4-B 附註）——原本那句裁示是在「資料放新加坡」的前提下做的，
    改到台灣機房後前提只會更寬鬆，文字不必動。
    > 決策歷程：**Cloudways 不適用**（代管環境為 PHP／MySQL，本系統需 Node.js 22＋PostgreSQL 16＋Docker）；
    > 2026-08-05 曾定 **DigitalOcean Droplet（Singapore）**，因 DO 無台灣機房而需接受資料出境，
    > 2026-08-06 改用 GCP 後此問題消失。
    > **用 VM 不用 Cloud Run**：現行 Docker Compose（app＋PostgreSQL＋備份＋提醒排程）可原封不動搬上去；
    > Cloud Run 要改成 Cloud SQL＋Cloud Scheduler＋Secret Manager，是另一套架構且更綁死單一雲。
16. **切換上線時的官網公告同步**：官網公告寫「超額系統自動取消第 3 筆、不另行通知」，
    自建系統為「當下擋下、不動既有預約」（院長 2026-08-01 裁示）。切換上線時該句需改寫，
    屬可見文字、需院長逐字核可。詳見 `09` §五。

## 四、與現有官網的關係

- 本系統位於 repo `booking-system/`，為**獨立部署**的 Next.js + PostgreSQL 應用；官網維持 Netlify 靜態站不變。
- `_redirects` 第 5 節已加入 `/booking-system/*` 強制 404，避免 Netlify 把原始碼當靜態檔公開。
- 官網可於「線上預約」按鈕連到本系統網址；BookNow 停用時程由診所決定，兩者可並行過渡。
