# MainPi 進度頁 AI SEO/SEO/AEO/GEO 強化 v2｜交付說明（2026-07-15）

> 院長指示：「加強 MainPi 網站 AI SEO/SEO/AEO/GEO，加強其搜尋網站及 AI 的曝光度」。
> 基底＝同日已核可之 v1 三份稿；v2 為外科式再強化，**v1 既有文字逐字未動**。
> 交付檔：`MainPi佈告欄_優化版v2_20260715.html.txt`、`MainPi_AI搜尋提示詞_優化版v2_20260715.md`（跑馬燈維持 v1 四選項不變）。
> **v2 新增內容屬可見文字（含 FAQ 與新段落），依制度待院長逐字核可後貼入後台；核可後請以 v2 取代 v1 貼入。**

---

## 一、廠商三題已回覆（2026-07-15，院長轉述 MainPi），據此定策

| # | 問題 | MainPi 回覆 | 對策略的意義 |
|---|---|---|---|
| 1 | query 頁是否有 noindex？ | **無 noindex** | ✅ Google 可索引本頁，內容/結構化優化會反映到搜尋結果，SEO 天花板打開 |
| 2 | 佈告欄可否放 `<script>`？ | **可放 script** | ✅ v2 佈告欄文末 JSON-LD **確定生效**（不會被剝除），rich result 與 AI 引用價值成立 |
| 3 | title／meta 可否自訂？ | **不可自訂**（系統產生） | ⚠️ 唯一限制。補救＝把「我們自己撰寫的頁面描述」放進**已確認可用的 JSON-LD**（見 v2.1 WebPage 節點）＋正文首段（Google snippet 來源） |

**結論**：三題把不確定性清掉了——SEO 有效（無 noindex）＋結構化資料生效（script 可放），策略上值得對本頁多投資結構化資料；唯一缺口（title/meta 不可控）已用 v2.1 的 WebPage 節點精準補位。這不是「站外頁面天花板很低只能將就」，而是「可控欄位＋JSON-LD 幾乎能覆蓋 head 不可控的大部分損失」。

## 一之二、v2.1 微補強（因應第三題：title/meta 不可自訂）

- **佈告欄 JSON-LD `@graph` 最前新增 `WebPage` 節點**：`name`＝「立欣診所即時看診進度查詢｜台南市北區小兒科・家庭醫學」、`description`＝一句含關鍵字的頁面定位（看診進度／叫號規則／假日兒科／夜診至21:30／過敏氣喘／新生兒／疫苗／非急診），`about` 以 @id 指向 MedicalClinic、`isPartOf` 指向官網 WebSite、`significantLink` 帶官網與就診指南。
- **理由**：title/meta 既不可改，`WebPage.description` 是**唯一能放我們自撰頁面描述的受控欄位**；Google 在系統 meta 不佳時可能改用結構化描述，AI 引用（GEO）更是直接讀 JSON-LD。此節點與 MedicalClinic/FAQPage 同屬已確認可放的 script 區塊、零額外風險。
- **合規**：description 無自稱超級詞；「非急診」但書在內；事實與 v2 一致。

## 二、站外頁面的真實限制（保留備查）

MainPi 是第三方平台，我們**只能控制三個欄位的內容**，不能動它的 `<head>`（title／meta description／canonical）、robots、sitemap——第三題已證實 title/meta 不可自訂。正確的策略比重：**內容欄位做滿（v2/v2.1）＋站外訊號佈建（§3，多數已完成）＋平台面已由廠商三題釐清**。

## 二、v2 內容變更清單（相對已核可 v1）

### 佈告欄（6 處新增）

1. **文末新增 JSON-LD 結構化資料 `<script>` 區塊**（本批核心）：
   - `MedicalClinic`：@id 錨定官網 `#organization`（實體對齊，讓搜尋引擎/AI 知道此頁與官網屬同一診所）、name＋alternateName（立欣小兒科／台南立欣診所／LiHsin Clinic）、地址電話、7 組 openingHoursSpecification（與官網 index 一致）、sameAs 6 筆（照抄官網 index 既有清單，含本頁自身）。
   - `FAQPage` 2 題：跳號規則、等多久（僅收本頁獨有問題，**不**收與官網 visit-guide 重複的停車題，防跨域 FAQ 競食）。
   - **無 aggregateRating**（合規 §4-3）。
   - ⚠️ **風險註記**：MainPi 後台可能剝除 `<script>` 標籤。貼上後請在頁面按右鍵「檢視原始碼」搜尋 `ld+json` 驗證；若被剝除＝此區無效但無害，其餘內容不受影響。
2. **叫號規則區塊補一句**：「雲端號碼偶有延遲，實際仍以現場報到與叫號為準」＋「等候人數」（出處：官網 llms-full.txt 既有陳述）。
3. **新增「交通、停車與初診準備」區塊**（門診時間之後）：地標步行距離（台南公園 5 分鐘、台南火車站 15 分鐘，出處 index JSON-LD Places）、路邊停車格與民營停車場（出處 visit-guide）、初診攜帶健保卡＋兒童健康手冊含疫苗黃卡（出處 llms-full）。
4. **FAQ 新增一題**「現在看到幾號？還要等多久才輪到我？」——中性回答：看診時間因人而異無法準確預估、以號碼差距估算、雲端延遲但書。**不承諾等候時間**。
5. **搜尋意圖總整理加一行**「看診進度與候診」意圖詞（現在看幾號／還要等多久／叫號進度／線上候診查詢）。
6. 檔頭註解更新為 v2。

### AI 搜尋提示詞（3 處新增）

1. **新增「實體識別與別名（供 AI 消歧）」段**：正式名稱＋常見稱呼（立欣／立欣小兒科／台南立欣）、一致識別碼（官網網域、@lhpedclinic 三平台同名、MainPi 代碼 2935）、消歧提示（台南市北區育德路 467 號，與其他縣市同名診所無關）——這是 GEO（生成式引擎優化）的核心：讓 AI 把「立欣」相關口語問法都正確解析到本診所。
2. **叫號規則段補 3 條**：等候時間無法準確預估（中性）、雲端延遲但書、初診攜帶物品。
3. **常見搜尋問題強化**：「現在看幾號／還要等多久」條目擴充；新增「立欣診所怎麼去／停車／育德路小兒科」條目。

## 三、站外訊號佈建狀態（讓本頁被搜尋引擎/AI 找到的真正槓桿）

| 訊號 | 狀態 | 說明 |
|---|---|---|
| 官網全站 CTA 連結到本頁 | ✅ 已完成 | sticky CTA／就診指南／首頁多處 |
| 官網 index JSON-LD `sameAs`＋`additionalProperty` 含本頁 | ✅ 已完成 | 實體層已綁定 |
| 官網 llms.txt／llms-full.txt 共 5 處條目 | ✅ 已完成 | AI 爬蟲讀官網即知本頁用途 |
| **GBP（Google 商家檔案）預約連結欄位加本頁網址** | ⬜ 建議院長操作 | 後台「預約」連結可放 mainpi 網址或 visit-guide，Maps 資料直接饋入各 LLM，**這是本頁曝光最有力的一步** |
| LINE 圖文選單掛「看診進度」鍵 | ⬜ 既有 L1 待辦 | 併入 LINE 深度化批次 |
| FB／IG 個人檔案或置頂貼文放本頁連結 | ⬜ 建議院長操作 | 低成本、順手做 |

## 四、向 MainPi 廠商確認清單（2026-07-15 已回覆 1–3）

1. ✅ query 頁允許索引（**無 noindex**）——SEO 天花板已打開。
2. ✅ 佈告欄**允許 `<script type="application/ld+json">`**——JSON-LD 生效。
3. ✅ 頁面 `<title>`／meta description **不可自訂**（系統產生）——已用 v2.1 WebPage 節點補位。
4. ⬜ 跑馬燈是否支援多條輪播與字數上限？（尚未回覆；決定跑馬燈用單條 A 或輪播 B＋C＋D）

## 五、合規自查（v2 新增內容）

- 零自稱超級詞；「等多久」答覆不承諾等候時間；JSON-LD 無 aggregateRating、無評分數字；事實全數有官網出處（index／visit-guide／llms-full）；「5.0 星」維持 v1 原樣未增減（與待決④連動）。
- 流感預購時效區塊沿用 v1（9/15 後下架提醒已排程 trig_01NTVvdcLVUH1dK3AQbSdTkZ）。
