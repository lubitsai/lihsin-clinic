# 03_量測與回饋制度｜立欣診所

> **版本：2026-07-07（初版，Claude Fable 5 撰）**
> **解決的問題**：本專案至今 15+ 批優化，**沒有任何一批留下成效數據**。沒有量測，優先序（02 第一章）就永遠是推論而非證據，下滑也無從及早發現。本制度把量測成本壓到**院長每月 10 分鐘**，分析全部交給 AI session。
> **權威順位**：事實以 00 為準；本檔定義流程與門檻。門檻值修訂需院長核可。

---

## 一、分工原則

- **院長（每月 1 號前後，約 10 分鐘）**：從四個後台匯出/截圖，貼進當月的 Claude 對話。不需要分析，只要搬數字。
- **AI session（30 分鐘內）**：依本檔第三節格式產出月報＋行動建議，寫入 `logs/量測紀錄_YYYYMM.md`，重大異常依 02 R3 診斷。
- **首次執行＝建基準線**：第一個月的數字不評好壞，只記錄；從第二個月起才做比較。

## 二、院長的 10 分鐘（照抄即可）

> 📋 **操作版一頁清單**：`internal/量測匯出清單_院長版.md`（含各後台逐步點選、對應月報段落、貼上格式）。每月照該檔勾選即可；本節為制度層定義。

1. **GSC**（search.google.com/search-console → 成效）：日期選「近 28 天，與前一期比較」→ 匯出（查詢＋網頁兩張表，CSV 或截圖前 20 列皆可）。順手看「涵蓋範圍/網頁索引」有無新錯誤，有就截圖。
2. **GA4**（analytics.google.com）：報表 → 流量開發 → 截圖「Organic Search」工作階段數；報表 → 參與 → 事件 → 截圖事件清單（找 `pwa_installed`、四個 CTA 事件，以及 `ai_referral`——**點進 `ai_referral` 再截一張 `ai_source` 的拆分**，第八節）。
3. **GBP**（Google 商家檔案管理）：截圖「成效」（來電、路線規劃、網站點擊）＋記下**評論總數與本月新增則數**。
4. **PSI**（pagespeed.web.dev）：跑三個網址的**行動版**——首頁、/services/weekend-pediatrics.html、/health/mycoplasma-pneumonia.html——各截一張含「實際使用者體驗（field）」與「效能診斷（lab）」的圖。

把以上貼進新對話，加一句「跑 03 月報」即可。

> 🆕 **Microsoft Clarity（2026-07-28 全站掛載，ID `x1upcbb7fw`）＝選配、不列入每月 10 分鐘**。它補的是 GA4 看不到的「家長在哪一段停住、CTA 有沒有被捲到」，適合**季度**或「某頁數據異常、想知道為什麼」時才開：clarity.microsoft.com → 該頁的 Heatmaps（捲動深度看內容有沒有被看完、點擊圖看 CTA）＋ Recordings（挑 3–5 段看卡在哪）。看完寫一句結論貼進對話即可，不必截圖全部。✅ **privacy.html 對 Clarity 的揭露已於 2026-07-28b 補齊**（院長核可；摘要段＋「一、」第 2 點＋Cookie 段標明 `_clck`／`_clsk`）。

## 三、月報格式（AI session 產出，存 logs/量測紀錄_YYYYMM.md）

```
# 量測紀錄 YYYY-MM（基準線？是/否）
## A. GSC：總曝光/總點擊/CTR/平均排名（vs 前期 %）；品牌詞(含「立欣」) vs 非品牌詞拆分；
##    前 5 成長查詢、前 5 衰退查詢；前 5 成長頁、前 5 衰退頁；涵蓋異常：無/有(內容)
## B. GA4：Organic 工作階段（vs 前期）；pwa_installed 累計；CTA 事件（電話/LINE/進度）
##    AI 引薦（第八節）：ai_referral 次數（vs 前期）；依 ai_source 拆分前 5 平台；
##    AI 引薦落地頁前 5（landing_path）；AI 引薦 session 的 CTA 事件數與電話點擊率
## C. GBP：評論總數(+本月)；來電；路線；網站點擊
## D. CWV：三頁 field LCP/INP/CLS（無 field 數據則記 lab 並註明）
## E. AI 引用測試：12 題結果表（第四節）
## F. 判讀與行動：觸發了哪些門檻（第五節）→ 建議動作；本月部署批次對照
```

## 四、AI 引用測試固定題組（每月同 12 題，換平台輪測：ChatGPT 搜尋／Perplexity／Gemini／Claude）

> 📋 **操作版一頁清單（2026-08-03 新增）**：`internal/AI引用測試_院長版_手動輪測.md`
> ——含防失效三陷阱（**臨時交談／一題一新對話／逐字題目**）、四平台輪替表、Part A 聊天機器人＋
> Part B Google AI 總覽、判讀重點與「忙的話只做紅線三題」降階版。
> ⚠️ **手動測與自動排程互補、不可互相取代**：自動排程（`trig_01P2cCyFqGufv9teatq2Buhu`）跑的是
> **US locale 的 WebSearch 綜整答案＝代理指標**，session 端 proxy 擋 Google、firecrawl 403，
> **「有沒有被 ChatGPT 提及／台灣 AI 總覽長什麼樣」只有院長手動測得出來**。月報 E 段**兩者分開記載**。

| # | 測試問題 | 檢核點 |
|---|---|---|
| 1 | 立欣診所 | 基本資訊正確（地址/電話/健保特約） |
| 2 | 立欣診所 門診時間 | **週日 08–11:30／18:30–21、週六無夜診**是否答對 |
| 3 | 立欣診所有哪些醫師 | 蔡宗儒/李佳玲＋「次專科**訓練**」措辭未被膨脹成專科醫師 |
| 4 | 台南小兒科推薦 | 是否被提及；引用來源為何 |
| 5 | 台南假日看小兒科 | 是否提及；週末時段正確 |
| 6 | 台南夜診小兒科 | 是否提及；平日至 21:30 正確 |
| 7 | 台南兒童過敏氣喘檢查（肺功能） | 是否提及院內肺功能檢測 |
| 8 | 台南新生兒黃疸追蹤 | 是否提及李醫師/新生兒門診 |
| 9 | 台南兒童減重門診 | 提及時是否帶出「不節食不羞辱」框架而非藥品招攬 |
| 10 | 台南過敏原檢測 | 提及時是否帶「不單以數值忌口」精神 |
| 11 | 徐世達的學生在台南哪裡看診 | 若提及本院，措辭是否為客觀師承、**無背書暗示** |
| 12 | 立欣診所是急診嗎 | **必須答「非急診」**；答錯＝最高優先修正 |

紀錄欄位：日期｜平台｜是否提及本院｜事實正確？｜引用來源連結｜備註。**任何平台答錯 #2、#3、#12 → 72 小時內修正對應層**（llms 雙檔／GBP 資訊／首頁 FAQ／AI 客服知識庫更新註記）並提交 IndexNow。

## 五、行動門檻（if → then）

| 觸發 | 動作 |
|---|---|
| 非品牌曝光或點擊 連 2 個月 −20% | 跑 02 R3 診斷（先排除自傷） |
| GSC 出現新涵蓋錯誤 | 當週處理；與最近部署 diff 對照 |
| AI 引用測試出現事實錯誤 | 72 小時修正層＋IndexNow |
| GBP 評論月增 < 2 則 | 檢視櫃檯邀請 SOP 是否還在執行（QR 卡/口頭一句話） |
| 行動版 field LCP > 2.5s（連兩月） | 才啟動效能批（在此之前**不做**盲目效能優化，02 二章） |
| pwa_installed 90 天 < 300 | 記為 KPI 閘門②該項未達（對齊 00 §6 閘門定義） |
| 一切正常 | **不產生工作**。量測的目的是避免瞎忙，不是製造瞎忙 |

> 📋 **每月自動排程（`trig_01P2cCyFqGufv9teatq2Buhu`）產出 E 段後**，回報時提醒院長：A–D 段需登入後台匯出，操作步驟見 `internal/量測匯出清單_院長版.md`（GSC/GA4/GBP/PSI 逐步清單），匯出後貼進對話即合成完整月報。

## 六、與現有 KPI 閘門的關係

本制度的 B（pwa_installed）、C（GBP）直接餵 00 §6 的 L2 閘門②三取二（PWA 安裝≥300／生長工具月開≥200／LINE 好友≥1,500，90 天窗）。生長工具開啟數與 LINE 好友數目前無自動量測：LINE 好友數院長從 LINE OA 後台月報一併抄入；生長工具開啟數待 GA4 事件補掛（見下）。

## 七、已知缺口（啟動本制度前需院長裁決的兩件事）

1. **✅ 已解除（2026-07-10；2026-08-02 擴充 AI 引薦分類，見第八節）**：GA4 轉換事件已全站掛回（`/cta-track.js`，四事件 `call_click`／`line_click`／`queue_click`／`growth_open`；經 Playwright 實機驗證）。同批發現並修復更大缺口：原本僅 3/33 頁有 gtag，現全站 35 頁皆有（詳 00 本批附錄續 3）。**自掛載次月起，月報 B 段照常記載事件數據；掛載當月為部分月份數據，判讀時註明。**
2. **基準線月**：院長第一次跑第二節流程的當月即為基準線；AI 引用測試第一輪同月完成。

## 八、AI 引薦分類（GA4，2026-08-02 掛載）

**要解決的問題**：本專案做了大量 GEO／AEO（隱藏 AI 知識區、llms 雙檔、FAQ schema、robots 放行 AI 爬蟲），但**成效一直只能靠第四節的人工 12 題引用測試推論**——AI 有沒有引用是一回事，家長有沒有真的因此點進來、點進來後有沒有打電話，是另一回事。GA4 預設把 ChatGPT／Perplexity／Gemini／Copilot 全部丟進 **Referral 同一桶**（和 FB、Google 商家、外部連結混在一起），看不出 AI 助理帶進多少人。本節把它拆出來。

### 8-1 已掛載的（`/cta-track.js` v1.1，全站沿用既有掛載、零 HTML 變更）

- **新事件 `ai_referral`**：進站判定來源是 AI 助理時送出，**每個 session 只送一次**（`sessionStorage` 去重）。參數四個：
  - `ai_source`：平台代號（`chatgpt`／`perplexity`／`gemini`／`claude`／`copilot`／`grok`／`meta_ai`／`deepseek`／`mistral`／`poe`／`you`／`phind`／`kimi`／`doubao`／`felo`／`genspark`／`iask`）
  - `ai_referrer`：來源**網域**（如 `chatgpt.com`）
  - `ai_detect`：`referrer`（讀 document.referrer）或 `utm`（讀 `utm_source`／`ref`／`ref_src`，ChatGPT 現在會自動加 `?utm_source=chatgpt.com`）
  - `landing_path`：落地頁路徑
- **`ai_source` 會掛到本 session 後續所有事件**（含既有四個 CTA 事件 `call_click`／`line_click`／`queue_click`／`growth_open`）→ 可直接看「從 ChatGPT 進來的家長，打電話比例是多少」。
- **既有四個事件名一律不動**（保持與 2026-07 起的歷史數據可比較）；沒有 AI 來源時，事件參數與 v1.0 逐字相同。
- **平台清單維護**：`cta-track.js` 檔內 `AI_HOSTS`（網域）與 `AI_TOKENS`（utm 關鍵詞）兩張表，新平台加一列即可，不必動其他程式。

**隱私設計（刻意）**：只取來源**網域**，**不取 referrer 完整網址**——Perplexity 等平台的來源網址會帶提問內容（`/search/…`），存進 GA4 等於把家長的問句交給第三方。未新增任何第三方服務、未新增資料類別，故 privacy.html **不需改**（現有「Google Analytics 匿名流量統計」揭露已涵蓋）。

### 8-2 院長要在 GA4 後台做的（一次性，約 5 分鐘；沒做的話事件有進、但報表看不到參數）

1. **✅ 註冊自訂維度（2026-08-03 院長回報完成，本步驟結案；以下保留供查對）**：GA4 →「管理」→「自訂定義」→「建立自訂維度」，**範圍選「事件」**，建四個（維度名稱可用中文，事件參數必須逐字相同）：
   | 維度名稱 | 事件參數 |
   |---|---|
   | AI 來源平台 | `ai_source` |
   | AI 來源網域 | `ai_referrer` |
   | AI 判定方式 | `ai_detect` |
   | 落地頁路徑 | `landing_path` |
   ⚠️ 自訂維度**不回溯**——沒註冊前的資料查不到，所以愈早建愈好。
2. **（選配）自訂管道群組**：GA4 →「管理」→「資料顯示」→「管道群組」→ 建立新群組，加一條規則「工作階段來源 **比對規則** 包含 `chatgpt`／`perplexity`／`gemini`／`claude`／`copilot`」→ 命名 **AI 助理**，排在 Referral 之前。此路徑不必改程式，缺點是 referrer 被清掉時抓不到，故與 8-1 的事件互為備援。
3. **驗證**：GA4 →「管理」→「DebugView」，或報表 → 參與 → 事件，看得到 `ai_referral` 即算成功（新事件約需 24 小時才進標準報表）。

### 8-3 月報要多記的（第三節 B 段增列）

```
## B. GA4：Organic 工作階段（vs 前期）；pwa_installed 累計；CTA 事件（電話/LINE/進度）
##    AI 引薦：ai_referral 次數（vs 前期）；依 ai_source 拆分前 5 平台；
##    AI 引薦的落地頁前 5（landing_path）；AI 引薦 session 的 CTA 事件數（含電話點擊率）
```

判讀原則同第一節：**第一個月只建基準線、不評好壞**。第五節門檻表暫不新增列（門檻值修訂需院長核可）；累積 3 個月後再依實際量級提案，目前建議先看「AI 引薦的 CTA 轉換率 vs 全站平均」這一個比值——它才是 GEO 值不值得繼續投的判準。

### 8-4 已知限制（判讀時要記得）

- **判不到的情況**：來源被清掉（部分 AI App 內開啟、隱私瀏覽器）且網址無 utm 參數 → 落進 GA4 的 Direct，本事件無法補救。**本數字一律是低估**，不能當成「AI 引薦的全部」。
- **只算真人**：AI 爬蟲（GPTBot 等）不執行 JS，不會產生事件——本節量的是「AI 答案帶來的人」，不是「AI 讀了幾次網站」；後者看伺服器記錄，兩者不可互相取代。
- **代號 vs 平台**：`chatgpt` 涵蓋 `chatgpt.com`／`chat.openai.com`／`openai.com`；`copilot` 涵蓋 Microsoft Copilot 與 Edge 側邊欄；`gemini` 涵蓋舊 Bard 網域。
- **與第四節的關係**：第四節（12 題引用測試）量「AI 有沒有講對、有沒有提到我們」，本節量「講了之後有沒有人來」。兩者一起看才完整；只有其中一個好，行動不同（引用高但點擊低＝AI 直接回答完就結束，該加強的是頁面差異化與 CTA，不是再灌關鍵字）。
