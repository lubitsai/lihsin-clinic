# SOP：門診異動與門診時間更新

> **用途**：立欣診所官網「門診時間相關變更」的標準作業流程。涵蓋兩種情境，動手前**先判別屬於哪一種**，再照對應清單執行。
> **權威順位**：本 SOP 是執行手冊；事實與合規規則仍以 `00_專案總覽索引.md`（§4 合規、§8 文件維護）為準，判準以 `02_優化決策手冊.md`（R4 dateModified）為準。兩者衝突時以 00／02 為準並回修本檔。
> **重要限制**：`validate_site.py` **不檢查門診時間一致性**（只驗 HTML/JSON-LD 結構、合規禁語）。因此情境 B 的全站同步**全靠本清單防漏**，漏一處就會前後矛盾（對家長誤導、對 SEO/AEO 是硬傷）。

---

## ⭐ 六載體同步清單（院長 2026-08-23 指示，**每次門診異動都要走完**）

> 官網只是其中一個載體。**家長會從哪裡看到門診時間，就要在哪裡改。**
> 本清單不分情境 A／B，**一律適用**；情境 B（常態變更）另加下方「常態變更加做」三項。
> **提醒義務（院長 2026-08-23 指示「下次門診異動再記得提醒以上六點」）——一次任務提醒兩次**：
> **①接到門診異動任務的第一則回覆就先列出六項**，不要等做完官網才講。理由：②～⑥ 是院長／櫃檯端的事，
> 早點知道就能和 Claude 改官網**並行**處理，而 ⑤④⑥ 都是「晚一小時就有家長白跑」的實害項。
> **②交付訊息結尾再附一次完整表**，逐項標 ✅（已做）／⬜（待院長做）。
> **不可只回報官網做完就結束**——②～⑥ session 內做不到，漏講等於漏做。

| # | 載體 | 誰做 | 動作 | 為什麼不能漏 |
|---|---|---|---|---|
| **①** | **官網「最新門診異動」** | **Claude** | 公告卡（圖＋alt＋說明＋`data-expires`）＋`EXCEPTIONS` 徽章特例＋dateModified／sitemap＋驗證＋部署 | 口徑基準，其他載體都對齊這裡 |
| **②** | **Facebook 貼文 ＋ 限時動態** | 院長 | 兩個都發：貼文留存可回查、限動當下觸及 | 只發限動 24 小時後就查不到 |
| **③** | **Instagram 限時動態** | 院長 | 發限動 | |
| **④** | **Google 商家檔案「特殊營業時間」** | 院長 | 設該日特殊時間（整日休診＝設「休息」；半日＝填實際起訖） | **影響最大**：Google 地圖／搜尋／AI 直接讀這裡回答「今天有開嗎」，不改就是對外說謊；且過期自動回歸常態、不必記得改回來 |
| **⑤** | **BookNow 預約時段** | 院長／櫃檯 | **只關閉休診當節的時段**；既有預約逐筆聯繫改期 | 不關就會有家長約到診所沒開的時段 |
| **⑥** | **診所門口實體公告** | 櫃檯 | 列印 A4 貼門口（`internal/tools/make_door_notice.py`）。**撕掉是櫃檯例行、不必另外提醒** | 已經出門、走到門口的家長只看得到這張——**最後一道防線**，也是唯一擋得住「沒在用網路的家長」的載體 |

**⑤的現況（2026-08-23 院長確認）**：線上跑的是 **BookNow**（`lhpedclinic.booknow.com.tw`），
**`booking-system/` 自建系統尚未切換**。repo 端的 `sync_schedule.py` 只是先把自建系統的
`schedule.json` 對齊官網，**現在還不是家長實際會約到的那一套**——別把它當成「⑤ 已完成」。
**切換到自建系統之後**，⑤ 改為自建系統的班表更新（屆時才輪到主機上的 `npx tsx scripts/sync-schedule.ts`，
先 `--dry-run` 看受影響預約名單）。

**建議順序**：⑤ → ④ → ⑥ → ① → ②③。
**⑤④⑥ 是「實害組」**——不做會讓家長約到空診次、被 Google 告知有開、或走到門口才發現沒開；
②③ 是觸及層，晚幾分鐘無妨。

**⑥ 怎麼做**：`python3 internal/tools/make_door_notice.py images/notice/clinic-notice-YYYYMMDD.jpg`
→ 產出 `internal/print/<檔名>-A4.pdf`（**A4 橫式** 300dpi，院長 2026-08-23 指定；加 `--portrait` 可改直式）。
正方形公告圖列印約 **190×190mm**，橫式直式一樣大（都受 A4 短邊 210mm 限制），**差別只在紙張方向與留白落在哪一側**。
**列印選「實際大小／100%」**，不要選「配合頁面縮放」。**PDF 上刻意不加任何文字**——公告圖本身資訊完整，
另寫字＝新增未經核可的可見文字，且容易與網路版對不齊。`internal/print/` 由 `_redirects` 擋在站外。

**常態變更（情境 B）加做**：
- **Google 商家檔案「一般營業時間」**（不是特殊營業時間）
- **MainPi 佈告欄／AI 提示詞**（稿在 `internal/mainpi/`）
- **Chatbase 知識庫**（正本由院長維護；Claude 只出更新註記檔）

**恢復日 Claude 什麼都不必提醒**（院長 2026-08-23 定案）：
- ①公告卡 `data-expires` 自動下架、④ 特殊營業時間自動過期 → 機制自理。
- **⑤ BookNow 不需要開回來**：只關掉休診當節的那些時段，**不會動到其他診次、
  也不影響之後同一星期幾的班表**，所以沒有「恢復」這個動作。
  ⚠️ 這點與自建系統不同——自建系統改的是**週班表**（套用即對所有未來日期生效），
  單日異動要走 `EXCEPTIONS`／`SPECIAL_HOURS`，切換後別把 BookNow 的習慣直接套過去。
- **⑥ 門口那張撕掉＝櫃檯例行工作**，院長明示**不需要另外提醒**。

→ 所以提醒的範圍**只有異動當下的六項**，沒有「恢復日提醒」這回事。不要自作主張排恢復日的鬧鐘。

---

## 〇、先判別情境（決策樹）

```
門診時間有變 →
├─ 只是「某一天」臨時調整（醫師進修/國定假日/颱風/單日停診）
│   且下週起恢復原狀？
│        → 情境 A：臨時單日異動（公告圖 + EXCEPTIONS + 自動過期）
│
└─ 是「常態」改變固定週班表（新增/取消某時段、改起訖時間、改休診日）？
         → 情境 B：常態門診時間變更（改 SCHEDULE + 全站同步）
```

兩者可能同時發生（例：宣布下個月起週日改休診，本週日先停一次）——先照 B 改常態，再視需要用 A 補當週公告。

---

## 一、情境 A：臨時單日異動

**成果樣貌**：首頁「最新門診異動」區塊(`#clinic-notice`)換上新公告圖、HERO 徽章當日顯示異動後時段、到期自動下架。

### 步驟

1. **公告圖**（正方形）放入 `images/notice/`，**檔名帶日期**（如 `clinic-notice-20260819.webp`＋`.jpg`），避免同名檔被 CDN 快取舊圖。
   - **同一天的第二版海報要再加版次**（`clinic-notice-20260823b.*`）。CDN 以檔名為快取鍵，內容換了但檔名沒換＝家長重整仍看到舊圖（2026-08-23 教訓）。
   - **院長在對話貼圖時，原始檔就在 `/root/.claude/uploads/<session-id>/`——先 `ls` 那裡，不要急著重畫。**
     取得原檔後只做**轉檔**（Pillow：webp q84 ＋ jpg q88 後備，維持原生尺寸不放大），細節見 `infographic-upload` skill。
   - 真的找不到原檔才談重繪（Chromium 無頭渲染 HTML 截圖）；**重繪是最後手段**，容器只有 WenQuanYi Zen Hei 一種中文字型，
     做不出院長海報的手寫風標題與裝飾，成品與原稿明顯不同，**且院長要的是原圖**（2026-08-12 裁示）。
2. **先看現有公告還在不在效期內**（`grep -n 'notice-item' index.html` 看每則的 `data-expires`）：
   - **還在效期內 → 並列，不要覆蓋**。舊的原封不動留著。
     **排序依「適用日期」由近到遠，不是依「加入順序」**（2026-08-23c 定案）：當天仍在效期的公告排最前面，
     未來日期的往後排。這樣今天與明天都會給出正確的先後——近期那則自動下架後，下一則自然升到第一。
     （08-22c 的「新的在上」剛好兩種排法一致，故未區辨；08-23c 三則並列時才分得出來：
     8/23 那則講「今晚晚診休診」是此刻最急的，8/24 那則明天才生效。）
     2026-08-22c 就是這個情形：8/23 豪大雨臨時公告若覆蓋掉 9/1 新門診時間表公告（效期到 8/31），
     會讓 Chatbase 問答稿的口徑基準與 3 處 `#clinic-notice` 連結指到看不見的東西。
   - **已過期 → 直接換掉那則**（過期的公告留著也不會顯示，但別累積，換掉最乾淨）。
3. **改 `index.html` 的 `#clinic-notice` 區塊**（搜 `id="clinic-notice"` 定位）。每則公告＝一個
   `<div class="notice-item max-w-3xl mx-auto fade-in-up" data-expires="YYYY-MM-DD">`，內含五處要改：
   - 圖片路徑（該卡 `.notice-zoom` 內 `<picture>` 的 webp source 與 jpg img；**lightbox 不必改**，
     它是共用的 `<picture id="notice-lightbox-pic">`，內容由被點擊的卡動態填入）
   - `alt`（**公告全文逐字寫入**，爬蟲/螢幕閱讀器讀不到圖內文字）
   - 適用日期（`<time datetime>` 與可見「適用日期」文字）
   - 圖下可見說明文字（AEO：把圖內關鍵資訊重述一遍；圖上寫「正常門診」就把實際起訖時間補出來）
   - `data-expires="YYYY-MM-DD"`（該則適用末日，翌日起 UTC+8 自動隱藏**這一則**）
   並確認 `<section id="clinic-notice">` 自己的 `data-expires` ＝**所有公告中最晚的那個**（backstop：
   全部過期才整區隱藏）；卡與卡之間用 `mb-14` 留白，最後一則不加。
4. **若異動影響「當日開診時段」**（提早截止、當日某段停診、整日休診）→ 同步改 HERO 徽章的 `EXCEPTIONS` 表（`index.html` 內嵌 script，搜 `var EXCEPTIONS`）：
   - 鍵＝UTC+8 日期 `YYYY-MM-DD`，值＝**當日完整時段表**（整筆取代該日 `SCHEDULE`）
   - 格式：`[[開始分鐘,結束分鐘,'名稱'], …]`；分鐘＝時×60＋分（08:00=480、11:00=660、11:30=690、12:00=720、14:30=870、18:00=1080、18:30=1110、21:00=1260、21:30=1290）
   - **整日休診 = 空陣列 `[]`**（徽章自動顯示「目前休診，下個有診日…」）
   - **⚠️ 還沒定案的時段不要寫。** 公告若是「視情況滾動式調整、稍後公布」（颱風／豪雨常見），
     **先只放公告卡、`EXCEPTIONS` 留空**，等院長公布再補——寫「停」或「照常」都是猜，
     猜錯就是徽章對家長說謊。同時把「幾點公布、補哪一條」寫進 `00` 第六節待辦，別靠記憶。
   - 過期條目不影響運作，下次更新公告時順手清掉舊的即可
5. **收尾**（見 §三 共同尾段）：dateModified 跳 + sitemap 首頁 lastmod + 驗證 + Chromium 實測 + 部署。**臨時公告不進 llms**（時效資訊，過期即誤導 AI 引用）。

### 預設交付模式：即刻公告 + 立即部署（院長 2026-07-23 常設指示）

當**院長在對話直接貼上符合情境 A 的公告圖**時，預設一條龍走完、**不必逐次徵詢文案核可或部署確認**：

- **即刻公告**：公告區塊**不設起始日**，部署後即時顯示（現行實作即如此，只靠 `data-expires` 於「適用末日翌日」UTC+8 自動下架）。即使異動是未來日期，也是貼上當下就開始預告。
- **立即部署**：完成本節編修並通過 §三 驗證（`validate_site.py --stage deploy` ERROR 清零；動到徽章則 Chromium 實測四態）後，**直接快進合併進 `main` 並 `git push origin main`**（Netlify 由 `main` 建置上線），不再另開一輪問「文案可以嗎／要不要部署」。
- **文案處理**：公告可見文字（alt／圖下說明）就是**逐字轉述院長自製海報**，視同已核可；部署後於回覆中**附上實際上線之可見文案**供院長事後檢視。
- **仍不可放行的紅線**（照舊把關，發現即停下問院長）：合規禁語（`00 §4`，不得夾帶療效宣稱／自稱超級詞）；驗證器 ERROR；徽章實測未過。
- **⏰ 例外：院長明確指定發布時間時，以指定時間為準**（2026-08-23 先例：「請於今天早上 08:30 再發布」）。做法＝**先備妥並跑完全部驗證、commit 在 feature branch**，到指定時間才快進合併 `main`；用 `send_later`（claude-code-remote MCP）排一則自我提醒到點執行，不要靠 `sleep` 空等。
  **指定時間之後，院長中途回「Merge」一律當作「文案核可」、不是「立刻上線」**（2026-08-23 實例：07:38 回 Merge，確認後仍照 08:30 發布）。不確定就問一句，不要自行提前。
- **邊界**：本預設**僅限情境 A 的對話貼圖公告**。情境 B（常態門診時間變更）為多頁散改、屬可見醫療/營運資訊，**維持逐字核可**、不套用即刻部署。

> ⚠️ 本則覆寫 `CLAUDE.md`／`00` 對「可見文字逐字核可」在**情境 A 貼圖公告**這一狹義情形的前置卡關；合規紅線與驗證關卡不受影響。

---

## 二、情境 B：常態門診時間變更

**成果樣貌**：全站每一處門診時間敘述、結構化資料、徽章班表都一致改成新時間。

> ⚠️ **這是「一改要同步十幾處」的高風險工作。** 先跑探查指令抓出所有出現點，再逐一對照下表，最後回頭複跑探查指令確認沒有殘留舊時間。

### 探查指令（先跑，掌握全貌）

```bash
# 抓所有時段數字出現處（排除 archive 歷史檔）
grep -rnE '18:30|21:30|08:00|11:30|14:30|12:00' --include='*.html' --include='*.txt' . \
  | grep -v 'internal/archive'
# 抓固定敘述句（改時間時這類句子最容易漏）
grep -rn '平日夜診至21:30\|夜診至 21:30\|週六週日皆有門診' --include='*.html' --include='*.txt' . \
  | grep -v 'internal/archive'
```

### 必改清單（`index.html`）

| 位置 | 定位方式 | 內容 |
|---|---|---|
| **JSON-LD `openingHoursSpecification`** | 搜 `openingHoursSpecification`（約 165–212 行） | 機器可讀的門診時間權威來源。每段 `opens`/`closes`/`dayOfWeek`。**AI 與 Google 讀這裡，最優先改。** |
| **PropertyValue 描述** | 搜 `平日夜間門診`／`週六兒科時段`（約 502–505 行） | 「週一至週五 18:30–21:30」等文字值 |
| **首頁門診時間表格** | 搜 `id="schedule"`（約 2459–2544 行） | 可見表格，逐格時間 |
| **HERO 徽章 `SCHEDULE` 表** | 搜 `var SCHEDULE`（約 5269 行） | 開診狀態判斷來源。鍵 0=日、1–6=一～六；格式同 §一.3。改完 Chromium 實測。 |
| **FAQ／正文寫死時段** | 搜 `18:30`／`21:30`（約 1814、2588、3126、3905–4006 行等多處） | 各 FAQ 答案、正文段落 |
| **meta / og / twitter description** | 搜 `平日夜診至21:30`（14、22、36–45 行等） | 介紹層敘述 |

### 必改清單（其他頁面）

| 檔案 | 位置 | 內容 |
|---|---|---|
| `visit-guide.html` | JSON-LD description（約 784）、FAQ 答案（約 831）、meta desc（14/21/28） | 門診時間表與敘述 |
| `services/weekend-pediatrics.html` | JSON-LD `openingHoursSpecification`（約 78+）、description（約 62）、meta desc（7/16） | 假日/夜間時段（此頁專講假日夜診，務必改） |
| **其他 services/ 與 health/ 頁** | 探查指令列出的 meta desc | 多頁 desc 含「平日夜診至21:30」固定句 |
| `llms.txt`／`llms-full.txt` | 搜門診時間相關條目 | 提供給 LLM 的摘要，須同步 |

### 同一診次、不同醫師不同起訖（2026-08-12 起支援）

可見表一格可以放**多組「badge ＋ 時間」**，時間歸屬於它前面最近的那個 badge：

```html
<td class="p-3 text-center">
 <span class="badge-green" style="">蔡醫師</span>
 <p class="text-sm text-gray-600 mt-1 font-english">18:30–21:00</p>
 <span class="badge-green" style="background-color: rgb(255, 212, 211);">李醫師</span>
 <p class="text-sm text-gray-600 mt-1 font-english">18:30–21:30</p>
</td>
```

- **兩位醫師時間相同**就沿用舊寫法（單一 badge `蔡/李` ＋ 一組時間）；兩種寫法產出的 JSON 相同，換寫法不會平白製造 diff。
- **`SCHEDULE` 常數不要跟著拆**。它餵的是 HERO 開診徽章＝「診所這個時間開不開」，與醫師無關；
  比對時工具會自動把可見表收斂成診所級聯集（同診次取最早開始、最晚結束）再跟 `SCHEDULE` 對。
  上例的週一晚診聯集仍是 `18:30–21:30`，`SCHEDULE` 維持 `[1110,1290,'晚診']` 不動。
- 改完先跑 **`python3 internal/tools/sync_schedule.py --selftest`**（內建 2026-09-01 新表夾具＋負向測試，不讀 `index.html`、不寫檔），
  再跑 `--check`／不帶旗標的同步。
- 預約系統端：`schedule.json` 同一個 `session` 會出現兩筆，DB 的 `WeeklyScheduleTemplate` 唯一鍵是
  `[weekday, session, doctorId]`，本來就吃得下。單日例外是**診所級**的（沒有醫師欄位），
  `schedule-source.ts` 比對前會先收斂成一個診次一筆，否則同一個 `(date, session)` 會被推出兩筆重複的 `SPECIAL_HOURS`。

### 受影響預約怎麼抓（改常態班表必做）

- **覆蓋判斷是逐醫師的**（`isSlotCovered` 比對 `doctorId`）。所以**醫師對調也會被擋**，
  不是只有「時段縮短／取消」才會——某醫師在某診次的班被移除，約在那裡的預約就沒有對應區塊。
  **不要以為「時段還在就沒事」**（2026-08-12 曾這樣誤判，實測推翻）。
- **名單不必手動篩**：主機跑 `npx tsx scripts/sync-schedule.ts --dry-run`，
  印出完整名單（日期／時間／姓名／電話／預約編號）且不寫入資料庫（交易內套用後回滾）。
- **要在新班表生效當天跑，不要提早一天**。週班表沒有「生效日」欄位、套用即對所有未來日期生效，
  提早一天跑會把「前一天同一星期幾」的時段一起改掉。
- 名單沒清空前同步會整批擋下＝設計如此；**不要為了讓指令過就加 `--cancel-affected`**（見〈共同尾段〉第 3 點）。
- 要在不碰資料庫的情況下先推演，`findAppointmentsOutsideSchedule` 支援注入 `client`
  （`Pick<typeof prisma, "appointment" | "scheduleException">`），可餵合成預約逐種情況驗證。

### 站外（提醒院長，session 無法直接改）

- **Google 商家檔案**：營業時間（AI/地圖直接讀，影響最大）。
- **MainPi 佈告欄／AI 提示詞**：`internal/mainpi/` 有稿；門診時間變更時同步更新後請院長貼入後台。

### 收尾

見 §三 共同尾段。常態變更是**可見醫療/營運資訊**，dateModified 跳、需院長逐字核可、涉及多頁 → 部署後**補送 IndexNow**（`internal/tools/submit_indexnow.py`）。

---

## 三、共同尾段（兩情境都要做）

1. **dateModified／sitemap（R4）**：門診時間屬「家長讀到的營運資訊」→ 變更頁 `dateModified` 跳（完整 ISO 8601＋08:00）、`sitemap.xml` 對應 `lastmod` 同步。`lastReviewed` 不動（非醫師重審醫療內容）。判準見 `02` R4。
2. **合規**：時間敘述不得夾帶自稱超級詞、療效宣稱；紅線見 `00 §4`。
3. **驗證器**：`python3 internal/tools/validate_site.py --stage deploy` → **ERROR 清零才 push**。
4. **實機驗證**：改動 HERO 徽章（`SCHEDULE`/`EXCEPTIONS`）時，用 Chromium 模擬時間實測（腳本範例：`Date.now` 覆寫成目標時刻，讀 `#clinic-status-text`），確認「開診中/即將/休診/下個時段」四態正確、跨日正確、特例生效。
5. **院長核可**：可見文字（公告文案、表格時間、FAQ 敘述、徽章訊息）逐字經院長核可才 push（`00` 鐵律）。
6. **文件維護（§8）**：改 `00`/`01` 前先 `cp` 備份至 `archive/`；本批於 `00` 文末 append 附錄、檔頭更新版本行、`01` 同步。
7. **部署後**：內容實質變更批（尤其情境 B 多頁）→ 補送 IndexNow；情境 A 單頁公告可省。

---

## 四、常見錯誤（照做即可避開）

| 錯誤 | 後果 | 防法 |
|---|---|---|
| 情境 B 只改了首頁表格，漏改 JSON-LD | AI/Google 引用到舊時間 | 先跑探查指令、改完回頭複跑確認零殘留 |
| 公告圖用同檔名覆蓋 | CDN 快取舊圖、家長看到過期資訊 | 檔名一律帶日期；**同日改版再加版次 `b`／`c`** |
| 臨時異動只改公告圖、沒改 EXCEPTIONS | 徽章與公告矛盾（徽章仍顯示原時段） | §一.4：影響當日時段就加 EXCEPTIONS（**尚未定案的時段除外，見該節 ⚠️**） |
| 新公告直接覆蓋掉還在效期內的舊公告 | 舊公告（及指向它的客服稿／連結）憑空消失 | §一.2：先看 `data-expires`，仍在效期就並列 |
| 忘記設 data-expires | 過期公告一直掛首頁誤導家長 | §一.2：一律設 `data-expires` |
| 臨時公告寫進 llms | 過期後 AI 仍引用 | 臨時公告不進 llms |
| 改了時間沒跳 dateModified／sitemap | SEO 訊號過期 | §三.1（R4） |
