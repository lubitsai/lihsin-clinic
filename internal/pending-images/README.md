# internal/pending-images/ — 待上線圖檔暫存區（2026-09-03h 建）

**這個目錄裡的圖尚未上線，且不對外**（`/internal/*` 由 `_redirects` 第 5 節攔成 404）。

## 目前存放：`flu-vaccine-2026-*`（5 檔）

院長 2026-09-03 於對話交付的**新版懶人包圖「2026 自費流感疫苗資訊」**，
供 **2026-09-16 早鳥截止換版**使用。與現行線上版是同一設計、只拿掉時效層：

| | 現行線上版（預購版） | 本目錄（資訊版） |
|---|---|---|
| 主標 | 2026 自費流感疫苗**預購中** | 2026 自費流感疫苗**資訊** |
| 副標橫幅 | 即日起至 **9/15 早鳥預購享優惠** | **整條移除** |
| 左下區塊 | 流感疫苗**預購方式**（繳費／恕不退費） | 流感疫苗**接種資訊**（櫃檯洽詢／現場公告為準／醫師評估） |
| 5 張廠牌卡・頁尾 | — | **一字未動** |

來源恰 1254²、未裁切；以 `internal/tools/make_infographic.py` 產出、已目視驗過。

## ⚠️ 為什麼現在不掛上去

**9/15 前早鳥仍在跑。** 現在換圖會讓頁面內文「預購至 9/15」與圖自相矛盾，
且該頁現行 `alt`／`figcaption` 逐字描述的是舊圖（「即日起至 9/15 早鳥預購享優惠」），
換了圖那兩段文字就變成錯的描述。

## 9/16 怎麼用（規格全文見 `00` §6 標 🗓 那條的⑨）

```
cp internal/pending-images/flu-vaccine-2026-* images/
```

檔名與現行**完全相同** → HTML 的 `img src`／`srcset`／`og:image`／schema `image`
**一律不用動**；要改的只有 `alt` 與 `figcaption` 的文字（去時效字樣）。

**換完請刪除本目錄整個資料夾**，避免留下第 2 份會漂移的副本。
排程 `trig_015Bpq82orfoskWcddv3NQX9` 的 prompt 已載明以上全部。
